import { type FC } from 'react';

export const Header: FC = () => {
  return (
    <header className="pd-base flex h-[60px] items-center bg-gray-500/10 text-3xl font-bold text-amber-50">
      React Perfomance
    </header>
  );
};
