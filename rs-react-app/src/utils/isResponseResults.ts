// import type { Pokemons } from '@/services/api/types';

// export const isResponseResults = (obj: unknown): obj is Pokemons => {
//   return Array.isArray(obj) && 'name' in obj && 'url' in obj;
// };
